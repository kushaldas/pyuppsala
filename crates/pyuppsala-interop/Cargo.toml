[package]
name = "pyuppsala-interop"
version = "0.1.0"
edition = "2021"
rust-version = "1.77"
license = "BSD-2-Clause"
repository = "https://github.com/kushaldas/pyuppsala"
description = "Shared Rust interop types for pyuppsala extension consumers"
readme = "README.md"
keywords = ["xml", "python", "pyo3", "interop"]
categories = ["api-bindings"]

[dependencies]
uppsala = "=0.9.0"
